name: Heart_Disease_Classification

python_env: python_env.yaml

entry_points:
  main:
    parameters:
      model_type:
        type: str
        default: "all"
    command: "python modelling.py --model_type {model_type}"
